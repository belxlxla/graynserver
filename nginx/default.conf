server {
    listen 80;
    server_name graynchat.com;  # ← 본인 도메인으로 변경!

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://app:3000;
    }
}